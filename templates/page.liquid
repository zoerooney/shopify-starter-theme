<div class="page-title">
    <h1>{{ page.title }}</h1>
</div>
<div id="content">
    {% if page.content == null %}
        <div class="not-found">
            <h2>Coming soon...</h2>
        </div>
    {% else %}
        {{ page.content }}
    {% endif %}
</div><!-- #content -->